"""
The `app` package contains the real FastAPI service implementation.

Using an application package instead of a single flat script is a production-friendly habit because it:
- keeps routing, models, and service logic separate
- scales better as the codebase grows
- makes testing and future refactoring easier
"""

